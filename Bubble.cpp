/* * CSC340 Programming Methodology: In Class Exercise - Sorting Time * Authors: Anthony Portelli & Adolfo von Zastrow * File: Bubble.cpp * Created on August 4, 2015 */#include "bubble.h"Bubble::Bubble() : Sort() {};double Bubble::sort(){  time_point<system_clock> start, end;  duration<double> elapsed_seconds;  double total_runtime = 0.0;  int pass = 1;  bool sorted = false;  list<int>::iterator iterator = random_list->begin();  // start clock  start =  system_clock::now();  while (!sorted && (pass < number_of_items))  {    sorted = true;    iterator = random_list->begin();    for (int index = 0; index < number_of_items - pass; index++, iterator++)    {      list<int>::iterator next_iterator = iterator;      next_iterator++;      if (*iterator > *next_iterator)      {        iter_swap(iterator, next_iterator);        sorted = false;      }    }    pass++;  }  // stop clock  end =  system_clock::now();  // add runtime to total_runtime  elapsed_seconds = end - start;  return elapsed_seconds.count();}